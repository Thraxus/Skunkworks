namespace SkunkWorks.Thraxus.DataTypes.Enums
{
	public enum ExampleEnum
	{
		None, // Should always include none and have it at the top; it will be the default return if the enum is ever called without being explicitly set
		One, 
		Two,
		Apples
	}
}
